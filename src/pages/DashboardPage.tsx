export function DashboardPage() {
  return (
    <div>
      <h2 className="text-2xl font-semibold">Dashboard</h2>
      <p className="mt-2 text-muted-foreground">Manager metrics — implement with GET /api/dashboard.</p>
    </div>
  );
}

export function ClientsPage() {
  return (
    <div>
      <h2 className="text-2xl font-semibold">Clients</h2>
      <p className="mt-2 text-muted-foreground">Client list — implement with GET /api/clients.</p>
    </div>
  );
}
